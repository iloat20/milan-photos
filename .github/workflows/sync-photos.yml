name: Sync photo manifest

on:
  push:
    paths:
      - "photos/**"
      - "tools/sync_photos.py"
      - "tools/photos_lib.py"
      - ".github/workflows/sync-photos.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install deps
        run: pip install Pillow
      - name: Generate manifest + thumbs
        run: python tools/sync_photos.py
      - name: Commit if changed
        run: |
          if git diff --quiet -- photos/manifest.json photos/thumbs; then
            echo "manifest/thumbs unchanged"
          else
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add photos/manifest.json photos/thumbs
            git commit -m "chore: sync photos manifest and thumbs"
            git push
          fi
